import SysPermission from "@models/SysPermission";

class SysRoleService {
    public async fetchPermissionList () {
        // const sysPermissionList: SysPermission[] = await sysPermissionDB.list()
        const sysPermissionList: SysPermission[] = []
        return sysPermissionList
    }
}

export default SysRoleService